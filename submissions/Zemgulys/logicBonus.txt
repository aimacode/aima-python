60: The inference engine runs without exceptions*, and uses at least one rule to answer a query.
+10: The query "Hates(x, Jimmy)" uses exactly two rules to satisfy.
+5: The query "Hates(x, Roger)" uses a different sequence of two rules to satisfy.

