# Build-time toolchain for the JupyterLite proof-of-concept (see README.md).
# These are NOT runtime deps of aima; they only build the static browser site.
jupyterlite-core==0.8.0
jupyterlite-pyodide-kernel==0.8.1
jupyterlite==0.8.0
jupyter-server
build
